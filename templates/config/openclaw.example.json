{
  "$schema_note": "Minimal illustrative OpenClaw-style config fragment. Verify real fields against the official OpenClaw docs before copying into openclaw.json.",
  "models": {
    "default": "provider/model-name",
    "fallbacks": [
      "provider/backup-model"
    ]
  },
  "tools": {
    "exec": {
      "security": "deny",
      "ask": "always"
    }
  },
  "operator_notes": {
    "approval_gates": "Do not encode these as fictional config keys. Keep approval gates in your operating policy and use OpenClaw's real exec approvals/config surfaces for enforcement.",
    "never_publish": [
      "provider keys",
      "OAuth files",
      "channel IDs",
      "webhooks",
      "private endpoints"
    ]
  }
}
